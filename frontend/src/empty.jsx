// We declare this here and export it, so we can return empty arrays from
// selectors without re-rendering every time (because the object
// reference will be the same)
const EMPTY_ARRAY = [];

export {EMPTY_ARRAY};
